var React = require('react');

var Guest = React.createClass({
    render: function() {
        return (
            <div>
                Hello from Guest
            </div>
        )
    }
});

module.exports = Guest;
